import Layout from '../client/comps/Layout';
import Forms from '../client/comps/UserForms';

const Index = () => {
        return (
            <Layout>
                <Forms />
            </Layout>
        )
}

export default Index;
